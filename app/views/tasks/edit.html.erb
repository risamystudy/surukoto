<!--メニュー-->
<%= render 'users/menus', users: @users %>


<%= form_with(model: @task) do |f| %>
 <div class="form-group row justify-content-center">
    <div class="col-sm-7">
      <%= f.text_field :content, class: "form-control" %>
    </div>
      <%= f.submit '編集', class: 'btn btn-primary col-sm-2' %>
  </div>

<% end %>